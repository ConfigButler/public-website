apiVersion: platform.example/v1
kind: DatabaseClaim
metadata:
  name: checkout
  namespace: dev
spec:
  engine: postgres
  size: small
  ttl: 3d
status:
  phase: PullRequestOpened
  checks: [schema ok, policy ok]
  pullRequest: platform-config#217
